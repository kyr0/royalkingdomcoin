module.exports = {
    networks: {
        "live": {
            network_id: 1,
            host: "localhost",
            port: 8546   // Different than the default below
        }
    },
    rpc: {
        host: "localhost",
        port: 8545
    }
};
